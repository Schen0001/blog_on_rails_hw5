<%if false%>
<div class="card">
    <ul class="list-group list-group-flush">
        <% @all_posts.each do |p| %>
            <li class="list-group-item">
                <h4 style="color: #1D95FF;"><%= p.title %></h4> <br>
                <%= p.body %> <br> <br>
                <p>Commented at <b><%= p.created_at %></b></p>
            </li>
    </ul>
    <% end %>
</div>
<%end%>



<div class="posts-container">
    <% @all_posts.each do |p| %>
        <div class="card border-secondary mb-3" style="max-width: 35rem; margin: 10px;">

            <div class="card-body text-secondary">
                <ul class="list-group list-group-flush">

                    <li class="list-group-item">
                        <h4 style="color: #1D95FF;">
                            <%= p.title %>
                        </h4> <br>
                        <%= p.body %> <br> <br>
                            <p>Commented at <b><%= p.created_at %></b> </p>
                    </li>
                </ul>
            </div>

        </div>
        <% end %>
</div>
